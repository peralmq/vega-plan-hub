registry=https://registry.npmjs.org/
# react-day-picker@8 (its Calendar wrapper in src/components/ui/calendar.tsx
# is unused dead code, so it's pinned rather than bumped to the v9/v10
# rewrite — see p2-01-dependency-updates Decision Log) hard-peers
# date-fns@"^2.28.0 || ^3.0.0"; this repo is on date-fns@4. next-themes
# (the original reason this line existed) no longer needs it as of 0.4.6.
legacy-peer-deps=true
